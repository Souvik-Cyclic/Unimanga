# Database
MONGO_URI=your_mongodb_connection_string

# JWT
JWT_SECRET=your_jwt_secret_key

# Server
PORT=3000
NODE_ENV=production

# Swagger docs basic auth (leave unset to keep /docs open in development)
DOCS_USER=docs
DOCS_PASSWORD=change-this-docs-password
